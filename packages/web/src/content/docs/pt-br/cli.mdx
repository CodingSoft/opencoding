---
title: CLI
description: Opções e comandos da CLI do codingsoft.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do codingsoft, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
codingsoft
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o codingsoft programaticamente.

```bash
codingsoft run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do codingsoft.

```bash
codingsoft [project]
```

#### Opções

| Flag         | Curto | Descrição                                                                  |
| ------------ | ----- | -------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue a última sessão                                                   |
| `--session`  | `-s`  | ID da sessão para continuar                                                |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| `--prompt`   |       | Prompt a ser usado                                                         |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| `--agent`    |       | Agente a ser usado                                                         |
| `--port`     |       | Porta para escutar                                                         |
| `--hostname` |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do codingsoft também possui os seguintes comandos.

---

### agent

Gerencie agentes para o codingsoft.

```bash
codingsoft agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do codingsoft já em execução, iniciado via comandos `serve` ou `web`.

```bash
codingsoft attach [url]
```

Isso permite usar o TUI com um backend codingsoft remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
codingsoft web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
codingsoft attach http://10.20.30.40:4096
```

#### Opções

| Flag        | Curto | Descrição                                |
| ----------- | ----- | ---------------------------------------- |
| `--dir`     |       | Diretório de trabalho para iniciar o TUI |
| `--session` | `-s`  | ID da sessão para continuar              |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
codingsoft agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
codingsoft agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
codingsoft auth [command]
```

---

#### login

O codingsoft é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `codingsoft auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/codingsoft/auth.json`.

```bash
codingsoft auth login
```

Quando o codingsoft é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
codingsoft auth list
```

Ou a versão curta.

```bash
codingsoft auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
codingsoft auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
codingsoft github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
codingsoft github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
codingsoft github run
```

##### Opções

| Flag      | Descrição                                        |
| --------- | ------------------------------------------------ |
| `--event` | Evento simulado do GitHub para executar o agente |
| `--token` | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
codingsoft mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
codingsoft mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
codingsoft mcp list
```

Ou use a versão curta.

```bash
codingsoft mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
codingsoft mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
codingsoft mcp auth list
```

Ou use a versão curta.

```bash
codingsoft mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
codingsoft mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
codingsoft mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
codingsoft models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
codingsoft models anthropic
```

#### Opções

| Flag        | Descrição                                                             |
| ----------- | --------------------------------------------------------------------- |
| `--refresh` | Atualiza o cache de modelos a partir do models.dev                    |
| `--verbose` | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no codingsoft.

```bash
codingsoft models --refresh
```

---

### run

Execute o codingsoft em modo não interativo passando um prompt diretamente.

```bash
codingsoft run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "codingsoft run"
codingsoft run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `codingsoft serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
codingsoft serve

# Em outro terminal, execute comandos que se anexam a ele
codingsoft run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag         | Curto | Descrição                                                                     |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--command`  |       | O comando a ser executado, use mensagem para argumentos                       |
| `--continue` | `-c`  | Continue a última sessão                                                      |
| `--session`  | `-s`  | ID da sessão para continuar                                                   |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)    |
| `--share`    |       | Compartilhe a sessão                                                          |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                                 |
| `--agent`    |       | Agente a ser usado                                                            |
| `--file`     | `-f`  | Arquivo(s) a serem anexados à mensagem                                        |
| `--format`   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                     |
| `--title`    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)    |
| `--attach`   |       | Anexe a um servidor codingsoft em execução (por exemplo, http://localhost:4096) |
| `--port`     |       | Porta para o servidor local (padrão para porta aleatória)                     |

---

### serve

Inicie um servidor codingsoft sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
codingsoft serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do codingsoft sem a interface TUI. Defina `CODINGSOFT_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `codingsoft`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do codingsoft.

```bash
codingsoft session [command]
```

---

#### list

Liste todas as sessões do codingsoft.

```bash
codingsoft session list
```

##### Opções

| Flag          | Curto | Descrição                                 |
| ------------- | ----- | ----------------------------------------- |
| `--max-count` | `-n`  | Limitar às N sessões mais recentes        |
| `--format`    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do codingsoft.

```bash
codingsoft stats
```

#### Opções

| Flag        | Descrição                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `--days`    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| `--tools`   | Número de ferramentas a serem mostradas (todas)                                                      |
| `--models`  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| `--project` | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
codingsoft export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do codingsoft.

```bash
codingsoft import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do codingsoft.

```bash
codingsoft import session.json
codingsoft import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor codingsoft sem cabeça com uma interface web.

```bash
codingsoft web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o codingsoft através de uma interface web. Defina `CODINGSOFT_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `codingsoft`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
codingsoft acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag         | Descrição                 |
| ------------ | ------------------------- |
| `--cwd`      | Diretório de trabalho     |
| `--port`     | Porta para escutar        |
| `--hostname` | Nome do host para escutar |

---

### uninstall

Desinstale o codingsoft e remova todos os arquivos relacionados.

```bash
codingsoft uninstall
```

#### Opções

| Flag            | Curto | Descrição                                |
| --------------- | ----- | ---------------------------------------- |
| `--keep-config` | `-c`  | Manter arquivos de configuração          |
| `--keep-data`   | `-d`  | Manter dados de sessão e snapshots       |
| `--dry-run`     |       | Mostrar o que seria removido sem remover |
| `--force`       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o codingsoft para a versão mais recente ou uma versão específica.

```bash
codingsoft upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
codingsoft upgrade
```

Para atualizar para uma versão específica.

```bash
codingsoft upgrade v0.1.48
```

#### Opções

| Flag       | Curto | Descrição                                                        |
| ---------- | ----- | ---------------------------------------------------------------- |
| `--method` | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do codingsoft aceita as seguintes flags globais.

| Flag           | Curto | Descrição                               |
| -------------- | ----- | --------------------------------------- |
| `--help`       | `-h`  | Exibir ajuda                            |
| `--version`    | `-v`  | Imprimir número da versão               |
| `--print-logs` |       | Imprimir logs no stderr                 |
| `--log-level`  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O codingsoft pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `CODINGSOFT_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `CODINGSOFT_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `CODINGSOFT_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `CODINGSOFT_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `CODINGSOFT_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `CODINGSOFT_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `CODINGSOFT_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `CODINGSOFT_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `CODINGSOFT_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `CODINGSOFT_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `CODINGSOFT_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `CODINGSOFT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `CODINGSOFT_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `CODINGSOFT_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `CODINGSOFT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `CODINGSOFT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `CODINGSOFT_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `CODINGSOFT_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `CODINGSOFT_DISABLE_FILETIME_CHECK`     | boolean | Desabilitar verificação de tempo de arquivo para otimização           |
| `CODINGSOFT_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `CODINGSOFT_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `CODINGSOFT_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `CODINGSOFT_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `codingsoft`) |
| `CODINGSOFT_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `CODINGSOFT_EXPERIMENTAL`                         | boolean | Habilitar todos os recursos experimentais                 |
| `CODINGSOFT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `CODINGSOFT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `CODINGSOFT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `CODINGSOFT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `CODINGSOFT_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `CODINGSOFT_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `CODINGSOFT_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `CODINGSOFT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `CODINGSOFT_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `CODINGSOFT_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar TY LSP para arquivos python                     |
| `CODINGSOFT_EXPERIMENTAL_MARKDOWN`                | boolean | Habilitar recursos experimentais de markdown              |
| `CODINGSOFT_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
