---
title: CLI
description: CodingSoft CLI-alternativer og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CodingSoft CLI starter som standard [TUI](/docs/tui) når den kjøres uten noen argumenter.

```bash
codingsoft
```

Men den godtar også kommandoer som dokumentert på denne siden. Dette lar deg samhandle med CodingSoft programmatisk.

```bash
codingsoft run "Explain how closures work in JavaScript"
```

---

### tui

Start CodingSoft-terminalbrukergrensesnittet.

```bash
codingsoft [project]
```

#### Flagg

| Flagg        | Kort | Beskrivelse                                                              |
| ------------ | ---- | ------------------------------------------------------------------------ |
| `--continue` | `-c` | Fortsett siste økt                                                       |
| `--session`  | `-s` | Økt ID for å fortsette                                                   |
| `--fork`     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| `--prompt`   |      | Ledetekst som skal brukes                                                |
| `--model`    | `-m` | Modell å bruke i form av leverandør/modell                               |
| `--agent`    |      | Agent som skal brukes                                                    |
| `--port`     |      | Port å lytte på                                                          |
| `--hostname` |      | Vertsnavn å lytte på                                                     |

---

## Kommandoer

CodingSoft CLI har også følgende kommandoer.

---

### agent

Administrer agenter for CodingSoft.

```bash
codingsoft agent [command]
```

---

### attach

Koble en terminal til en allerede kjørende CodingSoft-backend-server startet via `serve` eller `web` kommandoer.

```bash
codingsoft attach [url]
```

Dette gjør det mulig å bruke TUI med en ekstern CodingSoft-backend. For eksempel:

```bash
# Start the backend server for web/mobile access
codingsoft web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
codingsoft attach http://10.20.30.40:4096
```

#### Flagg

| Flagg       | Kort | Beskrivelse                       |
| ----------- | ---- | --------------------------------- |
| `--dir`     |      | Arbeidskatalog for å starte TUI i |
| `--session` | `-s` | Økt ID for å fortsette            |

---

#### create

Opprett en ny agent med tilpasset konfigurasjon.

```bash
codingsoft agent create
```

Denne kommandoen vil veilede deg gjennom å lage en ny agent med en tilpasset systemmelding og verktøykonfigurasjon.

---

#### list

List opp alle tilgjengelige agenter.

```bash
codingsoft agent list
```

---

### auth

Kommando for administrasjon av påloggingsinformasjon og innlogging for leverandører.

```bash
codingsoft auth [command]
```

---

#### login

CodingSoft drives av leverandørlisten på [Models.dev](https://models.dev), så du kan bruke `codingsoft auth login` til å konfigurere API-nøkler for enhver leverandør du vil bruke. Dette er lagret i `~/.local/share/codingsoft/auth.json`.

```bash
codingsoft auth login
```

Når CodingSoft starter opp, laster den leverandørene fra filen med påloggingsinformasjon. Og hvis det er noen nøkler definert i miljøene dine eller en `.env`-fil i prosjektet ditt.

---

#### list

Viser alle de autentiserte leverandørene som er lagret i filen med påloggingsinformasjon.

```bash
codingsoft auth list
```

Eller bruk kortversjonen.

```bash
codingsoft auth ls
```

---

#### logout

Logger deg ut av en leverandør ved å fjerne den fra filen med påloggingsinformasjon.

```bash
codingsoft auth logout
```

---

### github

Administrer GitHub-agenten for depotautomatisering.

```bash
codingsoft github [command]
```

---

#### install

Installer GitHub-agenten i depotet ditt.

```bash
codingsoft github install
```

Dette setter opp den nødvendige GitHub Actions-arbeidsflyten og veileder deg gjennom konfigurasjonsprosessen. [Finn ut mer](/docs/github).

---

#### run

Kjør GitHub-agenten. Dette brukes vanligvis i GitHub Actions.

```bash
codingsoft github run
```

##### Flagg

| Flagg     | Beskrivelse                                  |
| --------- | -------------------------------------------- |
| `--event` | GitHub mock-hendelse agenten skal kjøres for |
| `--token` | GitHub personlig tilgangsnøkkel              |

---

### mcp

Administrer Model Context Protocol-servere.

```bash
codingsoft mcp [command]
```

---

#### add

Legg til en MCP-server til konfigurasjonen.

```bash
codingsoft mcp add
```

Denne kommandoen vil veilede deg gjennom å legge til enten en lokal eller ekstern MCP-server.

---

#### list

Liste alle konfigurerte MCP-servere og deres tilkoblingsstatus.

```bash
codingsoft mcp list
```

Eller bruk den korte versjonen.

```bash
codingsoft mcp ls
```

---

#### auth

Autentiser med en OAuth-aktivert MCP-server.

```bash
codingsoft mcp auth [name]
```

Hvis du ikke oppgir et servernavn, blir du bedt om å velge blant tilgjengelige OAuth-kompatible servere.

Du kan også liste opp OAuth-kompatible servere og deres autentiseringsstatus.

```bash
codingsoft mcp auth list
```

Eller bruk den korte versjonen.

```bash
codingsoft mcp auth ls
```

---

#### logout

Fjern OAuth-påloggingsinformasjon for en MCP-server.

```bash
codingsoft mcp logout [name]
```

---

#### debug

Feilsøk OAuth-tilkoblingsproblemer for en MCP-server.

```bash
codingsoft mcp debug <name>
```

---

### models

List opp alle tilgjengelige modeller fra konfigurerte leverandører.

```bash
codingsoft models [provider]
```

Denne kommandoen viser alle modeller som er tilgjengelige på tvers av dine konfigurerte leverandører i formatet `provider/model`.

Dette er nyttig for å finne ut det eksakte modellnavnet som skal brukes i [din konfigurasjon](/docs/config/).

Du kan eventuelt sende en leverandør ID for å filtrere modeller etter den leverandøren.

```bash
codingsoft models anthropic
```

#### Flagg

| Flagg       | Beskrivelse                                                         |
| ----------- | ------------------------------------------------------------------- |
| `--refresh` | Oppdater modellbufferen fra models.dev                              |
| `--verbose` | Bruk mer detaljert modellutdata (inkluderer metadata som kostnader) |

Bruk `--refresh`-flagget for å oppdatere den bufrede modelllisten. Dette er nyttig når nye modeller er lagt til en leverandør og du vil se dem i CodingSoft.

```bash
codingsoft models --refresh
```

---

### run

Kjør CodingSoft i ikke-interaktiv modus ved å sende en melding direkte.

```bash
codingsoft run [message..]
```

Dette er nyttig for skripting, automatisering eller når du vil ha et raskt svar uten å starte hele TUI. For eksempel.

```bash "codingsoft run"
codingsoft run Explain the use of context in Go
```

Du kan også koble til en kjørende `codingsoft serve`-forekomst for å unngå MCP-serveres kaldstarttid ved hver kjøring:

```bash
# Start a headless server in one terminal
codingsoft serve

# In another terminal, run commands that attach to it
codingsoft run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagg

| Flagg        | Kort | Beskrivelse                                                              |
| ------------ | ---- | ------------------------------------------------------------------------ |
| `--command`  |      | Kommandoen for å kjøre, bruk melding for args                            |
| `--continue` | `-c` | Fortsett siste økt                                                       |
| `--session`  | `-s` | Økt ID for å fortsette                                                   |
| `--fork`     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| `--share`    |      | Del økten                                                                |
| `--model`    | `-m` | Modell å bruke i form av leverandør/modell                               |
| `--agent`    |      | Agent å bruke                                                            |
| `--file`     | `-f` | Fil(er) som skal legges ved meldingen                                    |
| `--format`   |      | Format: standard (formatert) eller json (rå JSON hendelser)              |
| `--title`    |      | Tittel for økten (bruker avkortet ledetekst hvis ingen verdi er oppgitt) |
| `--attach`   |      | Koble til en kjørende CodingSoft-server (f.eks. http://localhost:4096)     |
| `--port`     |      | Port for den lokale serveren (standard til tilfeldig port)               |

---

### serve

Start en headless CodingSoft-server for API-tilgang. Sjekk ut [server-dokumentene](/docs/server) for hele HTTP-grensesnittet.

```bash
codingsoft serve
```

Dette starter en HTTP-server som gir API tilgang til CodingSoft-funksjonalitet uten TUI-grensesnittet. Sett `CODINGSOFT_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `codingsoft`).

#### Flagg

| Flagg        | Beskrivelse                                        |
| ------------ | -------------------------------------------------- |
| `--port`     | Port å lytte på                                    |
| `--hostname` | Vertsnavn å lytte på                               |
| `--mdns`     | Aktiver mDNS-oppdagelse                            |
| `--cors`     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### session

Administrer CodingSoft-økter.

```bash
codingsoft session [command]
```

---

#### list

List opp alle CodingSoft-økter.

```bash
codingsoft session list
```

##### Flagg

| Flagg         | Kort | Beskrivelse                              |
| ------------- | ---- | ---------------------------------------- |
| `--max-count` | `-n` | Begrens til N siste økter                |
| `--format`    |      | Utdataformat: tabell eller json (tabell) |

---

### stats

Vis tokenbruk og kostnadsstatistikk for CodingSoft-øktene dine.

```bash
codingsoft stats
```

#### Flagg

| Flagg       | Beskrivelse                                                                      |
| ----------- | -------------------------------------------------------------------------------- |
| `--days`    | Vis statistikk for de siste N dagene (hele tiden)                                |
| `--tools`   | Antall verktøy som skal vises (alle)                                             |
| `--models`  | Vis oversikt over modellbruk (skjult som standard). Gi et tall for å vise topp N |
| `--project` | Filtrer etter prosjekt (alle prosjekter, tom streng: gjeldende prosjekt)         |

---

### export

Eksporter øktdata som JSON.

```bash
codingsoft export [sessionID]
```

Hvis du ikke gir en økt ID, blir du bedt om å velge fra tilgjengelige økter.

---

### import

Importer øktdata fra en JSON-fil eller en CodingSoft share-URL.

```bash
codingsoft import <file>
```

Du kan importere fra en lokal fil eller en CodingSoft share-URL.

```bash
codingsoft import session.json
codingsoft import https://opncd.ai/s/abc123
```

---

### web

Start en headless CodingSoft-server med et webgrensesnitt.

```bash
codingsoft web
```

Dette starter en HTTP-server og åpner en nettleser for å få tilgang til CodingSoft via et nettgrensesnitt. Sett `CODINGSOFT_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `codingsoft`).

#### Flagg

| Flagg        | Beskrivelse                                        |
| ------------ | -------------------------------------------------- |
| `--port`     | Port å lytte på                                    |
| `--hostname` | Vertsnavn å lytte på                               |
| `--mdns`     | Aktiver mDNS-oppdagelse                            |
| `--cors`     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### acp

Start en ACP (Agent Client Protocol) server.

```bash
codingsoft acp
```

Denne kommandoen starter en ACP-server som kommuniserer via stdin/stdout ved å bruke nd-JSON.

#### Flagg

| Flagg        | Beskrivelse          |
| ------------ | -------------------- |
| `--cwd`      | Arbeidskatalog       |
| `--port`     | Port å lytte på      |
| `--hostname` | Vertsnavn å lytte på |

---

### uninstall

Avinstaller CodingSoft og fjern alle relaterte filer.

```bash
codingsoft uninstall
```

#### Flagg

| Flagg           | Kort | Beskrivelse                                   |
| --------------- | ---- | --------------------------------------------- |
| `--keep-config` | `-c` | Behold konfigurasjonsfiler                    |
| `--keep-data`   | `-d` | Behold øktdata og øyeblikksbilder             |
| `--dry-run`     |      | Vis hva som ville blitt fjernet uten å fjerne |
| `--force`       | `-f` | Hopp over bekreftelsesforespørsler            |

---

### upgrade

Oppdaterer CodingSoft til siste versjon eller en spesifikk versjon.

```bash
codingsoft upgrade [target]
```

For å oppgradere til siste versjon.

```bash
codingsoft upgrade
```

For å oppgradere til en bestemt versjon.

```bash
codingsoft upgrade v0.1.48
```

#### Flagg

| Flagg      | Kort | Beskrivelse                                                    |
| ---------- | ---- | -------------------------------------------------------------- |
| `--method` | `-m` | Installasjonsmetoden som ble brukt: curl, npm, pnpm, bun, brew |

---

## Globale flagg

CodingSoft CLI bruker følgende globale flagg.

| Flagg          | Kort | Beskrivelse                         |
| -------------- | ---- | ----------------------------------- |
| `--help`       | `-h` | Vis hjelp                           |
| `--version`    | `-v` | Skriv ut versjonsnummer             |
| `--print-logs` |      | Skriv ut logger til stderr          |
| `--log-level`  |      | Loggnivå (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

CodingSoft kan konfigureres ved hjelp av miljøvariabler.

| Variabel                              | Type   | Beskrivelse                                                           |
| ------------------------------------- | ------ | --------------------------------------------------------------------- |
| `CODINGSOFT_AUTO_SHARE`                 | boolsk | Del økter automatisk                                                  |
| `CODINGSOFT_GIT_BASH_PATH`              | streng | Bane til Git Bash-kjørbar på Windows                                  |
| `CODINGSOFT_CONFIG`                     | streng | Bane til konfigurasjonsfil                                            |
| `CODINGSOFT_TUI_CONFIG`                 | streng | Bane til TUI-konfigurasjonsfil                                        |
| `CODINGSOFT_CONFIG_DIR`                 | streng | Bane til konfigurasjonskatalog                                        |
| `CODINGSOFT_CONFIG_CONTENT`             | streng | Innebygd json-konfigurasjonsinnhold                                   |
| `CODINGSOFT_DISABLE_AUTOUPDATE`         | boolsk | Deaktiver automatiske oppdateringskontroller                          |
| `CODINGSOFT_DISABLE_PRUNE`              | boolsk | Deaktiver beskjæring av gamle data                                    |
| `CODINGSOFT_DISABLE_TERMINAL_TITLE`     | boolsk | Deaktiver automatiske terminaltitteloppdateringer                     |
| `CODINGSOFT_PERMISSION`                 | streng | Innebygd json-tillatelseskonfigurasjon                                |
| `CODINGSOFT_DISABLE_DEFAULT_PLUGINS`    | boolsk | Deaktiver standard plugins                                            |
| `CODINGSOFT_DISABLE_LSP_DOWNLOAD`       | boolsk | Deaktiver automatiske LSP servernedlastinger                          |
| `CODINGSOFT_ENABLE_EXPERIMENTAL_MODELS` | boolsk | Aktiver eksperimentelle modeller                                      |
| `CODINGSOFT_DISABLE_AUTOCOMPACT`        | boolsk | Deaktiver automatisk kontekstkomprimering                             |
| `CODINGSOFT_DISABLE_CLAUDE_CODE`        | boolsk | Deaktiver lesing fra `.claude` (spørsmål + ferdigheter)               |
| `CODINGSOFT_DISABLE_CLAUDE_CODE_PROMPT` | boolsk | Deaktiver lesing `~/.claude/CLAUDE.md`                                |
| `CODINGSOFT_DISABLE_CLAUDE_CODE_SKILLS` | boolsk | Deaktiver innlasting av `.claude/skills`                              |
| `CODINGSOFT_DISABLE_MODELS_FETCH`       | boolsk | Deaktiver henting av modeller fra eksterne kilder                     |
| `CODINGSOFT_FAKE_VCS`                   | streng | Falsk VCS-leverandør for testformål                                   |
| `CODINGSOFT_DISABLE_FILETIME_CHECK`     | boolsk | Deaktiver filtidskontroll for optimalisering                          |
| `CODINGSOFT_CLIENT`                     | streng | Klientidentifikator (standard til `cli`)                              |
| `CODINGSOFT_ENABLE_EXA`                 | boolsk | Aktiver Exa-nettsøkeverktøy                                           |
| `CODINGSOFT_SERVER_PASSWORD`            | streng | Aktiver grunnleggende autentisering for `serve`/`web`                 |
| `CODINGSOFT_SERVER_USERNAME`            | streng | Overstyr grunnleggende autentiseringsbrukernavn (standard `codingsoft`) |
| `CODINGSOFT_MODELS_URL`                 | streng | Egendefinert URL for henting av modellkonfigurasjon                   |

---

### Eksperimentelt

Disse miljøvariablene muliggjør eksperimentelle funksjoner som kan endres eller fjernes.

| Variabel                                        | Type   | Beskrivelse                                   |
| ----------------------------------------------- | ------ | --------------------------------------------- |
| `CODINGSOFT_EXPERIMENTAL`                         | boolsk | Aktiver alle eksperimentelle funksjoner       |
| `CODINGSOFT_EXPERIMENTAL_ICON_DISCOVERY`          | boolsk | Aktiver ikonoppdagelse                        |
| `CODINGSOFT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolsk | Deaktiver kopi ved valg i TUI                 |
| `CODINGSOFT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | tall   | Standard tidsavbrudd for bash-kommandoer i ms |
| `CODINGSOFT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | tall   | Maks Output Tokens for LLM-svar               |
| `CODINGSOFT_EXPERIMENTAL_FILEWATCHER`             | boolsk | Aktiver filovervåker for hele dir             |
| `CODINGSOFT_EXPERIMENTAL_OXFMT`                   | boolsk | Aktiver oxfmt formatter                       |
| `CODINGSOFT_EXPERIMENTAL_LSP_TOOL`                | boolsk | Aktiver eksperimentelt LSP-verktøy            |
| `CODINGSOFT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolsk | Deaktiver filovervåking                       |
| `CODINGSOFT_EXPERIMENTAL_EXA`                     | boolsk | Aktiver eksperimentelle Exa-funksjoner        |
| `CODINGSOFT_EXPERIMENTAL_LSP_TY`                  | boolsk | Aktiver TY LSP for python-filer               |
| `CODINGSOFT_EXPERIMENTAL_MARKDOWN`                | boolsk | Aktiver eksperimentelle Markdown-funksjoner   |
| `CODINGSOFT_EXPERIMENTAL_PLAN_MODE`               | boolsk | Aktiver planmodus                             |
